import { ArrowRight } from "iconsax-react";
function LoadMoreBtn({ size, height, width, ...props }) {
    return (
        <ArrowRight size="16" color="#FFFFFF"/>


    )
}

export default LoadMoreBtn;